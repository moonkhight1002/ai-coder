# Problems Folder

Put each problem in its own subdirectory.

Each problem directory should contain:

- `problem.md`
- `tests.json`

`tests.json` format:

```json
[
  {
    "name": "basic_case",
    "input": "3\n1 2 3\n",
    "expected": "6\n"
  }
]
```
